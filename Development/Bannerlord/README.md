# This folder contains Bannerlord DLLs for development
# Run the copy-dlls.ps1 script to populate this folder with DLLs from your game installation
# 
# Usage:
#   .\Development\copy-dlls.ps1 -GameFolder "PATH_TO_YOUR_BANNERLORD"
#
# Example:
#   .\Development\copy-dlls.ps1 -GameFolder "D:\SteamLibrary\steamapps\common\Mount & Blade II Bannerlord"
#
# Note: DLL files are NOT committed to the repository (they are in .gitignore)
